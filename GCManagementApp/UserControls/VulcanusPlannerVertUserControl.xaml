<UserControl x:Class="GCManagementApp.UserControls.VulcanusPlannerVertUserControl" x:Name="VulcanusPlannerUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GCManagementApp.UserControls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lib="http://schemas.thinkpixellab.com/2011/xaml/presentation"
             xmlns:converters="clr-namespace:GCManagementApp.Converters"
             xmlns:enums="clr-namespace:GCManagementApp.Enums"
             xmlns:static="clr-namespace:GCManagementApp.Static"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:behaviors="clr-namespace:GCManagementApp.Behaviors"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:properties="clr-namespace:GCManagementApp.Properties"
             xmlns:dd="urn:gong-wpf-dragdrop"
             mc:Ignorable="d"
             d:DesignHeight="960" d:DesignWidth="1920">
   <UserControl.Resources>
      <converters:HeroToImageConverter x:Key="HeroToImageConverter"/>
      <converters:ClassToImageConverter x:Key="ClassToImageConverter"/>
      <converters:AttributeToImageConverter x:Key="AttributeToImageConverter"/>
      <converters:LevelToProgressValueConverter x:Key="LevelToProgressValueConverter"/>

      <DataTemplate x:Key="DragAdorner">
         <Image Source="{Binding ImageName, Converter={StaticResource HeroToImageConverter}}" Width="40" Height="40" />
      </DataTemplate>
   </UserControl.Resources>
   <Grid Margin="20">
      <Grid.ColumnDefinitions>
         <ColumnDefinition Width="*"/>
         <ColumnDefinition Width="auto" />
         <ColumnDefinition Width="*" />
         <ColumnDefinition Width="auto" />
         <ColumnDefinition Width="500" MaxWidth="1100"/>
      </Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
         <RowDefinition Height="auto"/>
         <RowDefinition Height="*"/>
         <RowDefinition Height="auto"/>
      </Grid.RowDefinitions>
      <ScrollViewer Margin="10" Grid.Column="1" Grid.RowSpan="2" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" x:Name="VulcaTeamScroller">
         <Grid HorizontalAlignment="Center" x:Name="VulcaTeams" Background="{DynamicResource MaterialDesignPaper}">
            <Grid.ColumnDefinitions>
               <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
               <RowDefinition Height="auto" />
               <RowDefinition Height="auto" />
               <RowDefinition Height="auto" />
               <RowDefinition Height="auto" />
               <RowDefinition Height="auto" />
               <RowDefinition Height="auto" />
               <RowDefinition Height="auto" />
               <RowDefinition Height="auto" />
               <RowDefinition Height="auto" />
               <RowDefinition Height="auto" />
               <RowDefinition Height="auto" />
               <RowDefinition Height="auto" />
               <RowDefinition Height="auto" />
               <RowDefinition Height="auto" />
               <RowDefinition Height="auto" />
            </Grid.RowDefinitions>

            <!-- Attribute Titles -->
            <Grid Grid.Row="0" VerticalAlignment="Top" Margin="0,5,0,0">
               <TextBlock Text="Retribution Attribute" FontSize="20" HorizontalAlignment="Center"/>
            </Grid>
            <Grid Grid.Row="4" VerticalAlignment="Top" Margin="0,5,0,0">
               <TextBlock Text="Balance Attribute" FontSize="20" HorizontalAlignment="Center"/>
            </Grid>
            <Grid Grid.Row="8" VerticalAlignment="Top" Margin="0,5,0,0">
               <TextBlock Text="Life Attribute" FontSize="20" HorizontalAlignment="Center"/>
            </Grid>

            <!-- Vulcanus teams-->
            <!-- Retribution Attribute -->
            <Grid Grid.Row="1" Margin="5" Height="100" Width="880" HorizontalAlignment="Right" >
               <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="80" />
                  <ColumnDefinition />
                  <ColumnDefinition Width="30"/>
               </Grid.ColumnDefinitions>
               <Border Grid.ColumnSpan="1" Background="#E06666" />
               <Border BorderBrush="#E06666" Grid.ColumnSpan="2" BorderThickness="5" />
               <TextBlock Text="Phase 1" HorizontalAlignment="Center" Foreground="Black" FontSize="15" Margin="0,10,0,0"/>
               <Image Source="/GCManagementApp;component/Resources/Attributes/Red.png" Width="40" Margin="0,20,0,0"/>

               <ListBox Grid.Column="1" Background="{Binding ElementName=VulcanusPlannerUC, Path=Background}" Margin="0,5,5,5" ItemsSource="{Binding VulcanusTeam[0]}"                        
                                         dd:DragDrop.IsDragSource="True"
                                         dd:DragDrop.IsDropTarget="True"
                                         dd:DragDrop.DragAdornerTemplate="{StaticResource DragAdorner}"
                                         dd:DragDrop.DropHandler="{Binding}"
                                         dd:DragDrop.UseDefaultEffectDataTemplate="True">
                  <ListBox.ItemsPanel>
                     <ItemsPanelTemplate>
                        <StackPanel IsItemsHost="True" Orientation="Horizontal"/>
                     </ItemsPanelTemplate>
                  </ListBox.ItemsPanel>
                  <ListBox.ItemTemplate>
                     <DataTemplate>
                        <Grid>
                           <Grid.ColumnDefinitions>
                              <ColumnDefinition Width="35" />
                              <ColumnDefinition Width="70" />
                              <ColumnDefinition Width="35" />
                              <ColumnDefinition Width="35" />
                           </Grid.ColumnDefinitions>
                           <Grid.RowDefinitions>
                              <RowDefinition Height="35" />
                              <RowDefinition Height="35"/>
                           </Grid.RowDefinitions>

                           <Image Source="{Binding Hero.HeroClass, Converter={StaticResource ClassToImageConverter}}" Width="30" Height="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                           <Image Grid.Row="1" Source="{Binding Hero.HeroAttribute, Converter={StaticResource AttributeToImageConverter}}" Width="30" Height="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                           <Image Grid.Column="1" Grid.RowSpan="2" Source="{Binding ImageName, Converter={StaticResource HeroToImageConverter}}" Width="65" Height="65"/>

                           <local:SiCoresOpenTierListOverlay Grid.Column="2" Width="35" Height="35" SiLevel="{Binding SiLevel}" IsCoreOpen="{Binding IsCoreOpen}" />
                           <Label Grid.Column="2" Content="{Binding SiLevel}" ContentStringFormat="{x:Static properties:Resources.SIX}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10" />
                           <ProgressBar Grid.Column="2" Grid.Row="1" Style="{StaticResource MaterialDesignCircularProgressBar}" Width="35" Height="35" Foreground="{StaticResource ChaserBrush}" Value="{Binding ChaserLevel, Converter={StaticResource LevelToProgressValueConverter}, ConverterParameter={x:Static enums:GrowthLevelType.Chaser}}"/>
                           <Label Grid.Column="2" Grid.Row="1" Content="{Binding ChaserLevel}" ContentStringFormat="{x:Static properties:Resources.CLX}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10" />
                           <ProgressBar Grid.Column="3" Style="{StaticResource MaterialDesignCircularProgressBar}" Width="35" Height="35" Foreground="{StaticResource DescentBrush}" Value="{Binding DescentLevel, Converter={StaticResource LevelToProgressValueConverter}, ConverterParameter={x:Static enums:GrowthLevelType.Descent}}"/>
                           <Label Grid.Column="3" Content="{Binding DescentLevel}" ContentStringFormat="{x:Static properties:Resources.DX}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10"/>
                        </Grid>
                     </DataTemplate>
                  </ListBox.ItemTemplate>
               </ListBox>

               <Button Grid.Column="3" Command="{Binding ClearRankCommand}" CommandParameter="0" Style="{StaticResource MaterialDesignIconButton}" Height="32" Width="32" Margin="0,0" WindowChrome.IsHitTestVisibleInChrome="True" ToolTip="Clear team" Visibility="{Binding IsPrintMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                  <materialDesign:PackIcon Kind="Close"/>
               </Button>
            </Grid>
            <Grid Grid.Row="2" Margin="5" Height="100" Width="880"  HorizontalAlignment="Right">
               <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="80" />
                  <ColumnDefinition />
                  <ColumnDefinition Width="30"/>
               </Grid.ColumnDefinitions>
               <Border Grid.ColumnSpan="1" Background="#E06666" />
               <Border BorderBrush="#E06666" Grid.ColumnSpan="2" BorderThickness="5" />
               <TextBlock Text="Rank 2" HorizontalAlignment="Center" Foreground="Black" FontSize="15" Margin="0,10,0,0"/>
               <Image Source="/GCManagementApp;component/Resources/Attributes/Red.png" Width="40" Margin="0,20,0,0"/>

               <ListBox Grid.Column="1" Background="{Binding ElementName=VulcanusPlannerUC, Path=Background}" Margin="0,5,5,5" ItemsSource="{Binding VulcanusTeam[1]}"                        
                                         dd:DragDrop.IsDragSource="True"
                                         dd:DragDrop.IsDropTarget="True"
                                         dd:DragDrop.DragAdornerTemplate="{StaticResource DragAdorner}"
                                         dd:DragDrop.DropHandler="{Binding}"
                                         dd:DragDrop.UseDefaultEffectDataTemplate="True">
                  <ListBox.ItemsPanel>
                     <ItemsPanelTemplate>
                        <StackPanel IsItemsHost="True" Orientation="Horizontal"/>
                     </ItemsPanelTemplate>
                  </ListBox.ItemsPanel>
                  <ListBox.ItemTemplate>
                     <DataTemplate>
                        <Grid>
                           <Grid.ColumnDefinitions>
                              <ColumnDefinition Width="35" />
                              <ColumnDefinition Width="70" />
                              <ColumnDefinition Width="35" />
                              <ColumnDefinition Width="35" />
                           </Grid.ColumnDefinitions>
                           <Grid.RowDefinitions>
                              <RowDefinition Height="35" />
                              <RowDefinition Height="35"/>
                           </Grid.RowDefinitions>

                           <Image Source="{Binding Hero.HeroClass, Converter={StaticResource ClassToImageConverter}}" Width="30" Height="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                           <Image Grid.Row="1" Source="{Binding Hero.HeroAttribute, Converter={StaticResource AttributeToImageConverter}}" Width="30" Height="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                           <Image Grid.Column="1" Grid.RowSpan="2" Source="{Binding ImageName, Converter={StaticResource HeroToImageConverter}}" Width="65" Height="65"/>

                           <local:SiCoresOpenTierListOverlay Grid.Column="2" Width="35" Height="35" SiLevel="{Binding SiLevel}" IsCoreOpen="{Binding IsCoreOpen}" />
                           <Label Grid.Column="2" Content="{Binding SiLevel}" ContentStringFormat="{x:Static properties:Resources.SIX}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10" />
                           <ProgressBar Grid.Column="2" Grid.Row="1" Style="{StaticResource MaterialDesignCircularProgressBar}" Width="35" Height="35" Foreground="{StaticResource ChaserBrush}" Value="{Binding ChaserLevel, Converter={StaticResource LevelToProgressValueConverter}, ConverterParameter={x:Static enums:GrowthLevelType.Chaser}}"/>
                           <Label Grid.Column="2" Grid.Row="1" Content="{Binding ChaserLevel}" ContentStringFormat="{x:Static properties:Resources.CLX}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10" />
                           <ProgressBar Grid.Column="3" Style="{StaticResource MaterialDesignCircularProgressBar}" Width="35" Height="35" Foreground="{StaticResource DescentBrush}" Value="{Binding DescentLevel, Converter={StaticResource LevelToProgressValueConverter}, ConverterParameter={x:Static enums:GrowthLevelType.Descent}}"/>
                           <Label Grid.Column="3" Content="{Binding DescentLevel}" ContentStringFormat="{x:Static properties:Resources.DX}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10"/>
                        </Grid>
                     </DataTemplate>
                  </ListBox.ItemTemplate>
               </ListBox>

               <Button Grid.Column="3" Command="{Binding ClearRankCommand}" CommandParameter="1" Style="{StaticResource MaterialDesignIconButton}" Height="32" Width="32" Margin="0,0" WindowChrome.IsHitTestVisibleInChrome="True" ToolTip="Clear team" Visibility="{Binding IsPrintMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                  <materialDesign:PackIcon Kind="Close"/>
               </Button>
            </Grid>
            <Grid Grid.Row="3" Margin="5" Height="100" Width="880"  HorizontalAlignment="Right">
               <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="80" />
                  <ColumnDefinition />
                  <ColumnDefinition Width="30"/>
               </Grid.ColumnDefinitions>
               <Border Grid.ColumnSpan="1" Background="#E06666" />
               <Border BorderBrush="#E06666" Grid.ColumnSpan="2" BorderThickness="5" />
               <TextBlock Text="Rank 3" HorizontalAlignment="Center" Foreground="Black" FontSize="15" Margin="0,10,0,0"/>
               <Image Source="/GCManagementApp;component/Resources/Attributes/Red.png" Width="40" Margin="0,20,0,0"/>

               <ListBox Grid.Column="1" Background="{Binding ElementName=VulcanusPlannerUC, Path=Background}" Margin="0,5,5,5" ItemsSource="{Binding VulcanusTeam[2]}"                        
                                         dd:DragDrop.IsDragSource="True"
                                         dd:DragDrop.IsDropTarget="True"
                                         dd:DragDrop.DragAdornerTemplate="{StaticResource DragAdorner}"
                                         dd:DragDrop.DropHandler="{Binding}"
                                         dd:DragDrop.UseDefaultEffectDataTemplate="True">
                  <ListBox.ItemsPanel>
                     <ItemsPanelTemplate>
                        <StackPanel IsItemsHost="True" Orientation="Horizontal"/>
                     </ItemsPanelTemplate>
                  </ListBox.ItemsPanel>
                  <ListBox.ItemTemplate>
                     <DataTemplate>
                        <Grid>
                           <Grid.ColumnDefinitions>
                              <ColumnDefinition Width="35" />
                              <ColumnDefinition Width="70" />
                              <ColumnDefinition Width="35" />
                              <ColumnDefinition Width="35" />
                           </Grid.ColumnDefinitions>
                           <Grid.RowDefinitions>
                              <RowDefinition Height="35" />
                              <RowDefinition Height="35"/>
                           </Grid.RowDefinitions>

                           <Image Source="{Binding Hero.HeroClass, Converter={StaticResource ClassToImageConverter}}" Width="30" Height="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                           <Image Grid.Row="1" Source="{Binding Hero.HeroAttribute, Converter={StaticResource AttributeToImageConverter}}" Width="30" Height="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                           <Image Grid.Column="1" Grid.RowSpan="2" Source="{Binding ImageName, Converter={StaticResource HeroToImageConverter}}" Width="65" Height="65"/>

                           <local:SiCoresOpenTierListOverlay Grid.Column="2" Width="35" Height="35" SiLevel="{Binding SiLevel}" IsCoreOpen="{Binding IsCoreOpen}" />
                           <Label Grid.Column="2" Content="{Binding SiLevel}" ContentStringFormat="{x:Static properties:Resources.SIX}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10" />
                           <ProgressBar Grid.Column="2" Grid.Row="1" Style="{StaticResource MaterialDesignCircularProgressBar}" Width="35" Height="35" Foreground="{StaticResource ChaserBrush}" Value="{Binding ChaserLevel, Converter={StaticResource LevelToProgressValueConverter}, ConverterParameter={x:Static enums:GrowthLevelType.Chaser}}"/>
                           <Label Grid.Column="2" Grid.Row="1" Content="{Binding ChaserLevel}" ContentStringFormat="{x:Static properties:Resources.CLX}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10" />
                           <ProgressBar Grid.Column="3" Style="{StaticResource MaterialDesignCircularProgressBar}" Width="35" Height="35" Foreground="{StaticResource DescentBrush}" Value="{Binding DescentLevel, Converter={StaticResource LevelToProgressValueConverter}, ConverterParameter={x:Static enums:GrowthLevelType.Descent}}"/>
                           <Label Grid.Column="3" Content="{Binding DescentLevel}" ContentStringFormat="{x:Static properties:Resources.DX}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10"/>
                        </Grid>
                     </DataTemplate>
                  </ListBox.ItemTemplate>
               </ListBox>
               <Button Grid.Column="3" Command="{Binding ClearRankCommand}" CommandParameter="2" Style="{StaticResource MaterialDesignIconButton}" Height="32" Width="32" Margin="0,0" WindowChrome.IsHitTestVisibleInChrome="True" ToolTip="Clear team" Visibility="{Binding IsPrintMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                  <materialDesign:PackIcon Kind="Close"/>
               </Button>
            </Grid>
             
            <!-- Balance Attribute -->
             
            <Grid Grid.Row="5" Margin="5" Height="100" Width="880"  HorizontalAlignment="Right">
               <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="80" />
                  <ColumnDefinition />
                  <ColumnDefinition Width="30" />
               </Grid.ColumnDefinitions>
               <Border Grid.ColumnSpan="1" Background="#C9DAF8" />
               <Border BorderBrush="#C9DAF8" Grid.ColumnSpan="2" BorderThickness="5" />
               <TextBlock Text="Phase 1" HorizontalAlignment="Center" Foreground="Black" FontSize="15" Margin="0,10,0,0"/>
               <Image Source="/GCManagementApp;component/Resources/Attributes/Blue.png" Width="40" Margin="0,20,0,0"/>

               <ListBox Grid.Column="1" Background="{Binding ElementName=VulcanusPlannerUC, Path=Background}" Margin="0,5,5,5" ItemsSource="{Binding VulcanusTeam[3]}"                        
                                         dd:DragDrop.IsDragSource="True"
                                         dd:DragDrop.IsDropTarget="True"
                                         dd:DragDrop.DragAdornerTemplate="{StaticResource DragAdorner}"
                                         dd:DragDrop.DropHandler="{Binding}"
                                         dd:DragDrop.UseDefaultEffectDataTemplate="True">
                  <ListBox.ItemsPanel>
                     <ItemsPanelTemplate>
                        <StackPanel IsItemsHost="True" Orientation="Horizontal"/>
                     </ItemsPanelTemplate>
                  </ListBox.ItemsPanel>
                  <ListBox.ItemTemplate>
                     <DataTemplate>
                        <Grid>
                           <Grid.ColumnDefinitions>
                              <ColumnDefinition Width="35" />
                              <ColumnDefinition Width="70" />
                              <ColumnDefinition Width="35" />
                              <ColumnDefinition Width="35" />
                           </Grid.ColumnDefinitions>
                           <Grid.RowDefinitions>
                              <RowDefinition Height="35" />
                              <RowDefinition Height="35"/>
                           </Grid.RowDefinitions>

                           <Image Source="{Binding Hero.HeroClass, Converter={StaticResource ClassToImageConverter}}" Width="30" Height="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                           <Image Grid.Row="1" Source="{Binding Hero.HeroAttribute, Converter={StaticResource AttributeToImageConverter}}" Width="30" Height="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                           <Image Grid.Column="1" Grid.RowSpan="2" Source="{Binding ImageName, Converter={StaticResource HeroToImageConverter}}" Width="65" Height="65"/>

                           <local:SiCoresOpenTierListOverlay Grid.Column="2" Width="35" Height="35" SiLevel="{Binding SiLevel}" IsCoreOpen="{Binding IsCoreOpen}" />
                           <Label Grid.Column="2" Content="{Binding SiLevel}" ContentStringFormat="{x:Static properties:Resources.SIX}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10" />
                           <ProgressBar Grid.Column="2" Grid.Row="1" Style="{StaticResource MaterialDesignCircularProgressBar}" Width="35" Height="35" Foreground="{StaticResource ChaserBrush}" Value="{Binding ChaserLevel, Converter={StaticResource LevelToProgressValueConverter}, ConverterParameter={x:Static enums:GrowthLevelType.Chaser}}"/>
                           <Label Grid.Column="2" Grid.Row="1" Content="{Binding ChaserLevel}" ContentStringFormat="{x:Static properties:Resources.CLX}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10" />
                           <ProgressBar Grid.Column="3" Style="{StaticResource MaterialDesignCircularProgressBar}" Width="35" Height="35" Foreground="{StaticResource DescentBrush}" Value="{Binding DescentLevel, Converter={StaticResource LevelToProgressValueConverter}, ConverterParameter={x:Static enums:GrowthLevelType.Descent}}"/>
                           <Label Grid.Column="3" Content="{Binding DescentLevel}" ContentStringFormat="{x:Static properties:Resources.DX}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10"/>
                        </Grid>
                     </DataTemplate>
                  </ListBox.ItemTemplate>
               </ListBox>
               <Button Grid.Column="3" Command="{Binding ClearRankCommand}" CommandParameter="3" Style="{StaticResource MaterialDesignIconButton}" Height="32" Width="32" Margin="0,0" WindowChrome.IsHitTestVisibleInChrome="True" ToolTip="Clear team" Visibility="{Binding IsPrintMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                  <materialDesign:PackIcon Kind="Close"/>
               </Button>
            </Grid>
            <Grid Grid.Row="6" Margin="5" Height="100" Width="880"  HorizontalAlignment="Right">
               <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="80" />
                  <ColumnDefinition />
                  <ColumnDefinition Width="30" />
               </Grid.ColumnDefinitions>
               <Border Grid.ColumnSpan="1" Background="#C9DAF8" />
               <Border BorderBrush="#C9DAF8" Grid.ColumnSpan="2" BorderThickness="5" />
               <TextBlock Text="Phase 2" HorizontalAlignment="Center" Foreground="Black" FontSize="15" Margin="0,10,0,0"/>
               <Image Source="/GCManagementApp;component/Resources/Attributes/Blue.png" Width="40" Margin="0,20,0,0"/>

               <ListBox Grid.Column="1" Background="{Binding ElementName=VulcanusPlannerUC, Path=Background}" Margin="0,5,5,5" ItemsSource="{Binding VulcanusTeam[4]}"                        
                                         dd:DragDrop.IsDragSource="True"
                                         dd:DragDrop.IsDropTarget="True"
                                         dd:DragDrop.DragAdornerTemplate="{StaticResource DragAdorner}"
                                         dd:DragDrop.DropHandler="{Binding}"
                                         dd:DragDrop.UseDefaultEffectDataTemplate="True">
                  <ListBox.ItemsPanel>
                     <ItemsPanelTemplate>
                        <StackPanel IsItemsHost="True" Orientation="Horizontal"/>
                     </ItemsPanelTemplate>
                  </ListBox.ItemsPanel>
                  <ListBox.ItemTemplate>
                     <DataTemplate>
                        <Grid>
                           <Grid.ColumnDefinitions>
                              <ColumnDefinition Width="35" />
                              <ColumnDefinition Width="70" />
                              <ColumnDefinition Width="35" />
                              <ColumnDefinition Width="35" />
                           </Grid.ColumnDefinitions>
                           <Grid.RowDefinitions>
                              <RowDefinition Height="35" />
                              <RowDefinition Height="35"/>
                           </Grid.RowDefinitions>

                           <Image Source="{Binding Hero.HeroClass, Converter={StaticResource ClassToImageConverter}}" Width="30" Height="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                           <Image Grid.Row="1" Source="{Binding Hero.HeroAttribute, Converter={StaticResource AttributeToImageConverter}}" Width="30" Height="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                           <Image Grid.Column="1" Grid.RowSpan="2" Source="{Binding ImageName, Converter={StaticResource HeroToImageConverter}}" Width="65" Height="65"/>

                           <local:SiCoresOpenTierListOverlay Grid.Column="2" Width="35" Height="35" SiLevel="{Binding SiLevel}" IsCoreOpen="{Binding IsCoreOpen}" />
                           <Label Grid.Column="2" Content="{Binding SiLevel}" ContentStringFormat="{x:Static properties:Resources.SIX}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10" />
                           <ProgressBar Grid.Column="2" Grid.Row="1" Style="{StaticResource MaterialDesignCircularProgressBar}" Width="35" Height="35" Foreground="{StaticResource ChaserBrush}" Value="{Binding ChaserLevel, Converter={StaticResource LevelToProgressValueConverter}, ConverterParameter={x:Static enums:GrowthLevelType.Chaser}}"/>
                           <Label Grid.Column="2" Grid.Row="1" Content="{Binding ChaserLevel}" ContentStringFormat="{x:Static properties:Resources.CLX}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10" />
                           <ProgressBar Grid.Column="3" Style="{StaticResource MaterialDesignCircularProgressBar}" Width="35" Height="35" Foreground="{StaticResource DescentBrush}" Value="{Binding DescentLevel, Converter={StaticResource LevelToProgressValueConverter}, ConverterParameter={x:Static enums:GrowthLevelType.Descent}}"/>
                           <Label Grid.Column="3" Content="{Binding DescentLevel}" ContentStringFormat="{x:Static properties:Resources.DX}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10"/>
                        </Grid>
                     </DataTemplate>
                  </ListBox.ItemTemplate>
               </ListBox>
               <Button Grid.Column="3" Command="{Binding ClearRankCommand}" CommandParameter="4" Style="{StaticResource MaterialDesignIconButton}" Height="32" Width="32" Margin="0,0" WindowChrome.IsHitTestVisibleInChrome="True" ToolTip="Clear team" Visibility="{Binding IsPrintMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                  <materialDesign:PackIcon Kind="Close"/>
               </Button>
            </Grid>
            <Grid Grid.Row="7" Margin="5" Height="100" Width="880" HorizontalAlignment="Right" >
               <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="80" />
                  <ColumnDefinition />
                  <ColumnDefinition Width="30" />
               </Grid.ColumnDefinitions>
               <Border Grid.ColumnSpan="1" Background="#C9DAF8" />
               <Border BorderBrush="#C9DAF8" Grid.ColumnSpan="2" BorderThickness="5" />
               <TextBlock Text="Rank 6" HorizontalAlignment="Center" Foreground="Black" FontSize="15" Margin="0,10,0,0"/>
               <Image Source="/GCManagementApp;component/Resources/Attributes/Blue.png" Width="40" Margin="0,20,0,0"/>

               <ListBox Grid.Column="1" Background="{Binding ElementName=VulcanusPlannerUC, Path=Background}" Margin="0,5,5,5" ItemsSource="{Binding VulcanusTeam[5]}"                        
                                         dd:DragDrop.IsDragSource="True"
                                         dd:DragDrop.IsDropTarget="True"
                                         dd:DragDrop.DragAdornerTemplate="{StaticResource DragAdorner}"
                                         dd:DragDrop.DropHandler="{Binding}"
                                         dd:DragDrop.UseDefaultEffectDataTemplate="True">
                  <ListBox.ItemsPanel>
                     <ItemsPanelTemplate>
                        <StackPanel IsItemsHost="True" Orientation="Horizontal"/>
                     </ItemsPanelTemplate>
                  </ListBox.ItemsPanel>
                  <ListBox.ItemTemplate>
                     <DataTemplate>
                        <Grid>
                           <Grid.ColumnDefinitions>
                              <ColumnDefinition Width="35" />
                              <ColumnDefinition Width="70" />
                              <ColumnDefinition Width="35" />
                              <ColumnDefinition Width="35" />
                           </Grid.ColumnDefinitions>
                           <Grid.RowDefinitions>
                              <RowDefinition Height="35" />
                              <RowDefinition Height="35"/>
                           </Grid.RowDefinitions>

                           <Image Source="{Binding Hero.HeroClass, Converter={StaticResource ClassToImageConverter}}" Width="30" Height="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                           <Image Grid.Row="1" Source="{Binding Hero.HeroAttribute, Converter={StaticResource AttributeToImageConverter}}" Width="30" Height="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                           <Image Grid.Column="1" Grid.RowSpan="2" Source="{Binding ImageName, Converter={StaticResource HeroToImageConverter}}" Width="65" Height="65"/>

                           <local:SiCoresOpenTierListOverlay Grid.Column="2" Width="35" Height="35" SiLevel="{Binding SiLevel}" IsCoreOpen="{Binding IsCoreOpen}" />
                           <Label Grid.Column="2" Content="{Binding SiLevel}" ContentStringFormat="{x:Static properties:Resources.SIX}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10" />
                           <ProgressBar Grid.Column="2" Grid.Row="1" Style="{StaticResource MaterialDesignCircularProgressBar}" Width="35" Height="35" Foreground="{StaticResource ChaserBrush}" Value="{Binding ChaserLevel, Converter={StaticResource LevelToProgressValueConverter}, ConverterParameter={x:Static enums:GrowthLevelType.Chaser}}"/>
                           <Label Grid.Column="2" Grid.Row="1" Content="{Binding ChaserLevel}" ContentStringFormat="{x:Static properties:Resources.CLX}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10" />
                           <ProgressBar Grid.Column="3" Style="{StaticResource MaterialDesignCircularProgressBar}" Width="35" Height="35" Foreground="{StaticResource DescentBrush}" Value="{Binding DescentLevel, Converter={StaticResource LevelToProgressValueConverter}, ConverterParameter={x:Static enums:GrowthLevelType.Descent}}"/>
                           <Label Grid.Column="3" Content="{Binding DescentLevel}" ContentStringFormat="{x:Static properties:Resources.DX}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10"/>
                        </Grid>
                     </DataTemplate>
                  </ListBox.ItemTemplate>
               </ListBox>
               <Button Grid.Column="3" Command="{Binding ClearRankCommand}" CommandParameter="5" Style="{StaticResource MaterialDesignIconButton}" Height="32" Width="32" Margin="0,0" WindowChrome.IsHitTestVisibleInChrome="True" ToolTip="Clear team" Visibility="{Binding IsPrintMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                  <materialDesign:PackIcon Kind="Close"/>
               </Button>
            </Grid>

            <!-- Life Attribute -->

            <Grid Grid.Row="9" Margin="5" Height="100" Width="880" HorizontalAlignment="Right">
               <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="80" />
                  <ColumnDefinition />
                  <ColumnDefinition Width="30" />
               </Grid.ColumnDefinitions>
               <Border Grid.ColumnSpan="1" Background="#6AA84F" />
               <Border BorderBrush="#6AA84F" Grid.ColumnSpan="2" BorderThickness="5" />
               <TextBlock Text="Phase 1" HorizontalAlignment="Center" Foreground="Black" FontSize="15" Margin="0,10,0,0"/>
               <Image Source="/GCManagementApp;component/Resources/Attributes/Green.png" Width="40" Margin="0,20,0,0"/>

               <ListBox Grid.Column="1" Background="{Binding ElementName=VulcanusPlannerUC, Path=Background}" Margin="0,5,5,5" ItemsSource="{Binding VulcanusTeam[6]}"                        
                                         dd:DragDrop.IsDragSource="True"
                                         dd:DragDrop.IsDropTarget="True"
                                         dd:DragDrop.DragAdornerTemplate="{StaticResource DragAdorner}"
                                         dd:DragDrop.DropHandler="{Binding}"
                                         dd:DragDrop.UseDefaultEffectDataTemplate="True">
                  <ListBox.ItemsPanel>
                     <ItemsPanelTemplate>
                        <StackPanel IsItemsHost="True" Orientation="Horizontal"/>
                     </ItemsPanelTemplate>
                  </ListBox.ItemsPanel>
                  <ListBox.ItemTemplate>
                     <DataTemplate>
                        <Grid>
                           <Grid.ColumnDefinitions>
                              <ColumnDefinition Width="35" />
                              <ColumnDefinition Width="70" />
                              <ColumnDefinition Width="35" />
                              <ColumnDefinition Width="35" />
                           </Grid.ColumnDefinitions>
                           <Grid.RowDefinitions>
                              <RowDefinition Height="35" />
                              <RowDefinition Height="35"/>
                           </Grid.RowDefinitions>

                           <Image Source="{Binding Hero.HeroClass, Converter={StaticResource ClassToImageConverter}}" Width="30" Height="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                           <Image Grid.Row="1" Source="{Binding Hero.HeroAttribute, Converter={StaticResource AttributeToImageConverter}}" Width="30" Height="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                           <Image Grid.Column="1" Grid.RowSpan="2" Source="{Binding ImageName, Converter={StaticResource HeroToImageConverter}}" Width="65" Height="65"/>

                           <local:SiCoresOpenTierListOverlay Grid.Column="2" Width="35" Height="35" SiLevel="{Binding SiLevel}" IsCoreOpen="{Binding IsCoreOpen}" />
                           <Label Grid.Column="2" Content="{Binding SiLevel}" ContentStringFormat="{x:Static properties:Resources.SIX}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10" />
                           <ProgressBar Grid.Column="2" Grid.Row="1" Style="{StaticResource MaterialDesignCircularProgressBar}" Width="35" Height="35" Foreground="{StaticResource ChaserBrush}" Value="{Binding ChaserLevel, Converter={StaticResource LevelToProgressValueConverter}, ConverterParameter={x:Static enums:GrowthLevelType.Chaser}}"/>
                           <Label Grid.Column="2" Grid.Row="1" Content="{Binding ChaserLevel}" ContentStringFormat="{x:Static properties:Resources.CLX}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10" />
                           <ProgressBar Grid.Column="3" Style="{StaticResource MaterialDesignCircularProgressBar}" Width="35" Height="35" Foreground="{StaticResource DescentBrush}" Value="{Binding DescentLevel, Converter={StaticResource LevelToProgressValueConverter}, ConverterParameter={x:Static enums:GrowthLevelType.Descent}}"/>
                           <Label Grid.Column="3" Content="{Binding DescentLevel}" ContentStringFormat="{x:Static properties:Resources.DX}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10"/>
                        </Grid>
                     </DataTemplate>
                  </ListBox.ItemTemplate>
               </ListBox>
               <Button Grid.Column="3" Command="{Binding ClearRankCommand}" CommandParameter="6" Style="{StaticResource MaterialDesignIconButton}" Height="32" Width="32" Margin="0,0" WindowChrome.IsHitTestVisibleInChrome="True" ToolTip="Clear team" Visibility="{Binding IsPrintMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                  <materialDesign:PackIcon Kind="Close"/>
               </Button>
            </Grid>
            <Grid Grid.Row="10" Margin="5" Height="100" Width="880" HorizontalAlignment="Right" >
               <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="80" />
                  <ColumnDefinition />
                  <ColumnDefinition Width="30" />
               </Grid.ColumnDefinitions>
               <Border Grid.ColumnSpan="1" Background="#6AA84F" />
               <Border BorderBrush="#6AA84F" Grid.ColumnSpan="2" BorderThickness="5" />
               <TextBlock Text="Phase 2" HorizontalAlignment="Center" Foreground="Black" FontSize="15" Margin="0,10,0,0"/>
               <Image Source="/GCManagementApp;component/Resources/Attributes/Green.png" Width="40" Margin="0,20,0,0"/>

               <ListBox Grid.Column="1" Background="{Binding ElementName=VulcanusPlannerUC, Path=Background}" Margin="0,5,5,5" ItemsSource="{Binding VulcanusTeam[7]}"                        
                                         dd:DragDrop.IsDragSource="True"
                                         dd:DragDrop.IsDropTarget="True"
                                         dd:DragDrop.DragAdornerTemplate="{StaticResource DragAdorner}"
                                         dd:DragDrop.DropHandler="{Binding}"
                                         dd:DragDrop.UseDefaultEffectDataTemplate="True">
                  <ListBox.ItemsPanel>
                     <ItemsPanelTemplate>
                        <StackPanel IsItemsHost="True" Orientation="Horizontal"/>
                     </ItemsPanelTemplate>
                  </ListBox.ItemsPanel>
                  <ListBox.ItemTemplate>
                     <DataTemplate>
                        <Grid>
                           <Grid.ColumnDefinitions>
                              <ColumnDefinition Width="35" />
                              <ColumnDefinition Width="70" />
                              <ColumnDefinition Width="35" />
                              <ColumnDefinition Width="35" />
                           </Grid.ColumnDefinitions>
                           <Grid.RowDefinitions>
                              <RowDefinition Height="35" />
                              <RowDefinition Height="35"/>
                           </Grid.RowDefinitions>

                           <Image Source="{Binding Hero.HeroClass, Converter={StaticResource ClassToImageConverter}}" Width="30" Height="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                           <Image Grid.Row="1" Source="{Binding Hero.HeroAttribute, Converter={StaticResource AttributeToImageConverter}}" Width="30" Height="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                           <Image Grid.Column="1" Grid.RowSpan="2" Source="{Binding ImageName, Converter={StaticResource HeroToImageConverter}}" Width="65" Height="65"/>

                           <local:SiCoresOpenTierListOverlay Grid.Column="2" Width="35" Height="35" SiLevel="{Binding SiLevel}" IsCoreOpen="{Binding IsCoreOpen}" />
                           <Label Grid.Column="2" Content="{Binding SiLevel}" ContentStringFormat="{x:Static properties:Resources.SIX}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10" />
                           <ProgressBar Grid.Column="2" Grid.Row="1" Style="{StaticResource MaterialDesignCircularProgressBar}" Width="35" Height="35" Foreground="{StaticResource ChaserBrush}" Value="{Binding ChaserLevel, Converter={StaticResource LevelToProgressValueConverter}, ConverterParameter={x:Static enums:GrowthLevelType.Chaser}}"/>
                           <Label Grid.Column="2" Grid.Row="1" Content="{Binding ChaserLevel}" ContentStringFormat="{x:Static properties:Resources.CLX}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10" />
                            <ProgressBar Grid.Column="3" Style="{StaticResource MaterialDesignCircularProgressBar}" Width="35" Height="35" Foreground="{StaticResource DescentBrush}" Value="{Binding DescentLevel, Converter={StaticResource LevelToProgressValueConverter}, ConverterParameter={x:Static enums:GrowthLevelType.Descent}}"/>
                           <Label Grid.Column="3" Content="{Binding DescentLevel}" ContentStringFormat="{x:Static properties:Resources.DX}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10"/>
                       </Grid>
                     </DataTemplate>
                  </ListBox.ItemTemplate>
               </ListBox>
               <Button Grid.Column="3" Command="{Binding ClearRankCommand}" CommandParameter="7" Style="{StaticResource MaterialDesignIconButton}" Height="32" Width="32" Margin="0,0" WindowChrome.IsHitTestVisibleInChrome="True" ToolTip="Clear team" Visibility="{Binding IsPrintMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                  <materialDesign:PackIcon Kind="Close"/>
               </Button>
            </Grid>
            <Grid Grid.Row="12" Margin="5" Height="100" Width="880"  HorizontalAlignment="Right">
               <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="80" />
                  <ColumnDefinition />
                  <ColumnDefinition Width="30" />
               </Grid.ColumnDefinitions>
               <Border Grid.ColumnSpan="1" Background="#6AA84F" />
               <Border BorderBrush="#6AA84F" Grid.ColumnSpan="2" BorderThickness="5" />
               <TextBlock Text="Rank 9" HorizontalAlignment="Center" Foreground="Black" FontSize="15" Margin="0,10,0,0"/>
               <Image Source="/GCManagementApp;component/Resources/Attributes/Green.png" Width="40" Margin="0,20,0,0"/>

               <ListBox Grid.Column="1" Background="{Binding ElementName=VulcanusPlannerUC, Path=Background}" Margin="0,5,5,5" ItemsSource="{Binding VulcanusTeam[8]}"                        
                                         dd:DragDrop.IsDragSource="True"
                                         dd:DragDrop.IsDropTarget="True"
                                         dd:DragDrop.DragAdornerTemplate="{StaticResource DragAdorner}"
                                         dd:DragDrop.DropHandler="{Binding}"
                                         dd:DragDrop.UseDefaultEffectDataTemplate="True">
                  <ListBox.ItemsPanel>
                     <ItemsPanelTemplate>
                        <StackPanel IsItemsHost="True" Orientation="Horizontal"/>
                     </ItemsPanelTemplate>
                  </ListBox.ItemsPanel>
                  <ListBox.ItemTemplate>
                     <DataTemplate>
                        <Grid>
                           <Grid.ColumnDefinitions>
                              <ColumnDefinition Width="35" />
                              <ColumnDefinition Width="70" />
                              <ColumnDefinition Width="35" />
                              <ColumnDefinition Width="35" />
                           </Grid.ColumnDefinitions>
                           <Grid.RowDefinitions>
                              <RowDefinition Height="35" />
                              <RowDefinition Height="35"/>
                           </Grid.RowDefinitions>

                           <Image Source="{Binding Hero.HeroClass, Converter={StaticResource ClassToImageConverter}}" Width="30" Height="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                           <Image Grid.Row="1" Source="{Binding Hero.HeroAttribute, Converter={StaticResource AttributeToImageConverter}}" Width="30" Height="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                           <Image Grid.Column="1" Grid.RowSpan="2" Source="{Binding ImageName, Converter={StaticResource HeroToImageConverter}}" Width="65" Height="65"/>

                           <local:SiCoresOpenTierListOverlay Grid.Column="2" Width="35" Height="35" SiLevel="{Binding SiLevel}" IsCoreOpen="{Binding IsCoreOpen}" />
                           <Label Grid.Column="2" Content="{Binding SiLevel}" ContentStringFormat="{x:Static properties:Resources.SIX}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10" />
                           <ProgressBar Grid.Column="2" Grid.Row="1" Style="{StaticResource MaterialDesignCircularProgressBar}" Width="35" Height="35" Foreground="{StaticResource ChaserBrush}" Value="{Binding ChaserLevel, Converter={StaticResource LevelToProgressValueConverter}, ConverterParameter={x:Static enums:GrowthLevelType.Chaser}}"/>
                           <Label Grid.Column="2" Grid.Row="1" Content="{Binding ChaserLevel}" ContentStringFormat="{x:Static properties:Resources.CLX}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10" />
                           <ProgressBar Grid.Column="3" Style="{StaticResource MaterialDesignCircularProgressBar}" Width="35" Height="35" Foreground="{StaticResource DescentBrush}" Value="{Binding DescentLevel, Converter={StaticResource LevelToProgressValueConverter}, ConverterParameter={x:Static enums:GrowthLevelType.Descent}}"/>
                           <Label Grid.Column="3" Content="{Binding DescentLevel}" ContentStringFormat="{x:Static properties:Resources.DX}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10"/>
                        </Grid>
                     </DataTemplate>
                  </ListBox.ItemTemplate>
               </ListBox>
               <Button Grid.Column="3" Command="{Binding ClearRankCommand}" CommandParameter="8" Style="{StaticResource MaterialDesignIconButton}" Height="32" Width="32" Margin="0,0" WindowChrome.IsHitTestVisibleInChrome="True" ToolTip="Clear team" Visibility="{Binding IsPrintMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                  <materialDesign:PackIcon Kind="Close"/>
               </Button>
            </Grid>
         </Grid>
      </ScrollViewer>

      <Border Grid.Column="4" Grid.Row="0" BorderThickness="1" HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="15,0" BorderBrush="{StaticResource MaterialDesignBody}"/>
      <TextBlock Grid.Column="4" Grid.Row="0" Text="Available heroes (Drag and drop on any team):" FontSize="15" Margin="25,0" HorizontalAlignment="Left" Padding="10,0" Background="{DynamicResource MaterialDesignPaper}"/>

      <GridSplitter Grid.Column="3" Grid.RowSpan="2" Width="5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />

      <!-- Heroes -->
      <ListBox Grid.Column="4" Grid.Row="01" Margin="15,0,15,15" ItemsSource="{Binding HeroesView}"                        
                                         dd:DragDrop.IsDragSource="True"
                                         dd:DragDrop.IsDropTarget="True"
                                         dd:DragDrop.DragAdornerTemplate="{StaticResource DragAdorner}"
                                         dd:DragDrop.DropHandler="{Binding}"
                                         dd:DragDrop.UseDefaultEffectDataTemplate="True">
         <ListBox.ItemContainerStyle>
            <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                <Setter Property="HorizontalContentAlignment" Value="Right"/>
                <Setter Property="VerticalContentAlignment" Value="Top"/>
                <Setter Property="materialDesign:ListBoxItemAssist.ShowSelection" Value="True"/>
            </Style>
         </ListBox.ItemContainerStyle>
         <ListBox.ItemsPanel>
             <ItemsPanelTemplate>
                 <WrapPanel IsItemsHost="True" />
             </ItemsPanelTemplate>
         </ListBox.ItemsPanel>
         <ListBox.ItemTemplate>
            <DataTemplate>
               <Grid>
                  <Grid.ColumnDefinitions>
                     <ColumnDefinition Width="35" />
                     <ColumnDefinition Width="70" />
                     <ColumnDefinition Width="35" />
                     <ColumnDefinition Width="35" />
                  </Grid.ColumnDefinitions>
                  <Grid.RowDefinitions>
                     <RowDefinition Height="35" />
                     <RowDefinition Height="35"/>
                  </Grid.RowDefinitions>

                  <Image Source="{Binding Hero.HeroClass, Converter={StaticResource ClassToImageConverter}}" Width="30" Height="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                  <Image Grid.Row="1" Source="{Binding Hero.HeroAttribute, Converter={StaticResource AttributeToImageConverter}}" Width="30" Height="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                  <Image Grid.Column="1" Grid.RowSpan="2" Source="{Binding ImageName, Converter={StaticResource HeroToImageConverter}}" Width="65" Height="65"/>

                  <local:SiCoresOpenTierListOverlay Grid.Column="2" Width="35" Height="35" SiLevel="{Binding SiLevel}" IsCoreOpen="{Binding IsCoreOpen}" />
                  <Label Grid.Column="2" Content="{Binding SiLevel}" ContentStringFormat="{x:Static properties:Resources.SIX}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10" />
                  <ProgressBar Grid.Column="2" Grid.Row="1" Style="{StaticResource MaterialDesignCircularProgressBar}" Width="35" Height="35" Foreground="{StaticResource ChaserBrush}" Value="{Binding ChaserLevel, Converter={StaticResource LevelToProgressValueConverter}, ConverterParameter={x:Static enums:GrowthLevelType.Chaser}}"/>
                  <Label Grid.Column="2" Grid.Row="1" Content="{Binding ChaserLevel}" ContentStringFormat="{x:Static properties:Resources.CLX}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10" />
                  <ProgressBar Grid.Column="3" Style="{StaticResource MaterialDesignCircularProgressBar}" Width="35" Height="35" Foreground="{StaticResource DescentBrush}" Value="{Binding DescentLevel, Converter={StaticResource LevelToProgressValueConverter}, ConverterParameter={x:Static enums:GrowthLevelType.Descent}}"/>
                  <Label Grid.Column="3" Content="{Binding DescentLevel}" ContentStringFormat="{x:Static properties:Resources.DX}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10"/>
               </Grid>
            </DataTemplate>
         </ListBox.ItemTemplate>
      </ListBox>

      <StackPanel Grid.ColumnSpan="5" Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,10">
         <Button Content="Save" Command="{Binding SaveCommand}" />
         <Button Content="Clear all teams" Command="{Binding ClearAllCommand}" Margin="10,0,0,0" Style="{StaticResource MaterialDesignOutlinedSecondaryButton}"/>
         <Button Content="Save to clipboard" Command="{Binding SaveToClipboardCommand}" Margin="10,0,0,0" Style="{StaticResource MaterialDesignOutlinedSecondaryButton}"/>
      </StackPanel>
   </Grid>
</UserControl>
