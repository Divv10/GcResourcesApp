<Window x:Class="GCManagementApp.Windows.DataSyncSummaryWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:properties="clr-namespace:GCManagementApp.Properties"
             xmlns:conv="clr-namespace:GCManagementApp.Converters"
             xmlns:uc="clr-namespace:GCManagementApp.UserControls"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             Background="{DynamicResource MaterialDesignPaper}"
             WindowStyle="None"
             mc:Ignorable="d" Width="800" Height="600" >
   <WindowChrome.WindowChrome>
      <WindowChrome CaptionHeight="32" ResizeBorderThickness="{x:Static SystemParameters.WindowResizeBorderThickness}" />
   </WindowChrome.WindowChrome>
   <Window.Resources>
      <conv:ArtifactToTypeConverter x:Key="ArtifactToTypeConverter" />
      <conv:AccessoryToSetConverter x:Key="AccessoryToSetConverter" />
   </Window.Resources>
   <Grid Margin="10,40,10,10">
      <Grid.RowDefinitions>
         <RowDefinition Height="*"/>
         <RowDefinition Height="auto"/>
      </Grid.RowDefinitions>

      <DataGrid AutoGenerateColumns="False" CanUserAddRows="False" HeadersVisibility="All" ItemsSource="{Binding ScanResults}">
         <DataGrid.Columns>
            <DataGridCheckBoxColumn Binding="{Binding IsChecked, UpdateSourceTrigger=PropertyChanged}" EditingElementStyle="{StaticResource MaterialDesignDataGridCheckBoxColumnEditingStyle}" ElementStyle="{StaticResource MaterialDesignDataGridCheckBoxColumnStyle}" />
            <DataGridTextColumn Binding="{Binding Hero}" Header="{x:Static properties:Resources.Hero}" EditingElementStyle="{StaticResource MaterialDesignDataGridTextColumnEditingStyle}" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
            <DataGridTextColumn Binding="{Binding Level}" Header="{x:Static properties:Resources.Level}" EditingElementStyle="{StaticResource MaterialDesignDataGridTextColumnEditingStyle}" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
            <DataGridTextColumn Binding="{Binding Chaser}" Header="{x:Static properties:Resources.CL}" EditingElementStyle="{StaticResource MaterialDesignDataGridTextColumnEditingStyle}" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
            <DataGridTextColumn Binding="{Binding Si}" Header="{x:Static properties:Resources.SI}" EditingElementStyle="{StaticResource MaterialDesignDataGridTextColumnEditingStyle}" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
            <DataGridTextColumn Binding="{Binding Trans}" Header="{x:Static properties:Resources.T}" EditingElementStyle="{StaticResource MaterialDesignDataGridTextColumnEditingStyle}" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
            <DataGridTextColumn Binding="{Binding Descent}" Header="{x:Static properties:Resources.D}" EditingElementStyle="{StaticResource MaterialDesignDataGridTextColumnEditingStyle}" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
            <DataGridTextColumn Binding="{Binding Pet}" Header="{x:Static properties:Resources.Pet}" EditingElementStyle="{StaticResource MaterialDesignDataGridTextColumnEditingStyle}" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
            <DataGridTextColumn Binding="{Binding Bp}" Header="{x:Static properties:Resources.BP}" EditingElementStyle="{StaticResource MaterialDesignDataGridTextColumnEditingStyle}" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
            <DataGridTextColumn Binding="{Binding EwLevel}" Header="{x:Static properties:Resources.EW}" EditingElementStyle="{StaticResource MaterialDesignDataGridTextColumnEditingStyle}" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
            <DataGridTextColumn Binding="{Binding ArtiType, Converter={StaticResource ArtifactToTypeConverter}}" Header="{x:Static properties:Resources.ArtifactType}" EditingElementStyle="{StaticResource MaterialDesignDataGridCheckBoxColumnEditingStyle}" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
            <DataGridTextColumn Binding="{Binding ArtiTier}" Header="{x:Static properties:Resources.ArtifactTier}" EditingElementStyle="{StaticResource MaterialDesignDataGridTextColumnEditingStyle}" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
            <DataGridTextColumn Binding="{Binding ArtiLevel}" Header="{x:Static properties:Resources.ArtifactLevel}" EditingElementStyle="{StaticResource MaterialDesignDataGridTextColumnEditingStyle}" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
            <DataGridTextColumn Binding="{Binding RingType, Converter={StaticResource AccessoryToSetConverter}}" Header="{x:Static properties:Resources.RingType}" EditingElementStyle="{StaticResource MaterialDesignDataGridCheckBoxColumnEditingStyle}" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
            <DataGridTextColumn Binding="{Binding RingTier}" Header="{x:Static properties:Resources.RingTier}" EditingElementStyle="{StaticResource MaterialDesignDataGridTextColumnEditingStyle}" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
            <DataGridTextColumn Binding="{Binding RingLevel}" Header="{x:Static properties:Resources.RingLevel}" EditingElementStyle="{StaticResource MaterialDesignDataGridTextColumnEditingStyle}" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
            <DataGridTextColumn Binding="{Binding NecklaceType, Converter={StaticResource AccessoryToSetConverter}}" Header="{x:Static properties:Resources.NecklaceType}" EditingElementStyle="{StaticResource MaterialDesignDataGridCheckBoxColumnEditingStyle}" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
            <DataGridTextColumn Binding="{Binding NecklaceTier}" Header="{x:Static properties:Resources.NecklaceTier}" EditingElementStyle="{StaticResource MaterialDesignDataGridTextColumnEditingStyle}" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
            <DataGridTextColumn Binding="{Binding NecklaceLevel}" Header="{x:Static properties:Resources.NecklaceLevel}" EditingElementStyle="{StaticResource MaterialDesignDataGridTextColumnEditingStyle}" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
            <DataGridTextColumn Binding="{Binding EarringType, Converter={StaticResource AccessoryToSetConverter}}" Header="{x:Static properties:Resources.EarringType}" EditingElementStyle="{StaticResource MaterialDesignDataGridCheckBoxColumnEditingStyle}" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
            <DataGridTextColumn Binding="{Binding EarringTier}" Header="{x:Static properties:Resources.EarringTier}" EditingElementStyle="{StaticResource MaterialDesignDataGridTextColumnEditingStyle}" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
            <DataGridTextColumn Binding="{Binding EarringLevel}" Header="{x:Static properties:Resources.EarringLevel}" EditingElementStyle="{StaticResource MaterialDesignDataGridTextColumnEditingStyle}" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
         </DataGrid.Columns>
      </DataGrid>

      <StackPanel Grid.Row="99" Orientation="Horizontal" HorizontalAlignment="Center">
         <Button Grid.Row="99" Grid.ColumnSpan="2" Width="100" Content="{x:Static properties:Resources.Update}" Style="{StaticResource MaterialDesignRaisedButton}" Command="{Binding UpdateCommand}" Margin="5,0"/>
         <Button Grid.Row="99" Grid.ColumnSpan="2" Width="100" Content="{x:Static properties:Resources.Close}" Style="{StaticResource MaterialDesignOutlinedButton}" Command="{Binding CloseCommand}" Margin="5,0"/>
      </StackPanel>
   </Grid>
</Window>
